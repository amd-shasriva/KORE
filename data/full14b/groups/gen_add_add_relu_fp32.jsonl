{"task_id": "gen_add_add_relu_fp32", "parent_id": "6c1bae1e68dc9a4407cfb090ed8ab9e3ba3aef95c4928b55a9e0b22eeadde185", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    M, N = a.shape\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a, b, c, o, numel, BLOCK=BLOCK, num_warps=4)\n    return o", "wall_us": 152.10000000000002, "snr_db": 999.0, "rank": 0}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass.\nStructural change: treat the tensors as flat 1D contiguous buffers and stream\nlarge contiguous tiles across a 1D grid. This maximizes HBM coalescing and\nminimizes grid/addressing overhead for this memory-bound op.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, n_elements,\n                         BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    base = pid * BLOCK\n    offs = base + tl.arange(0, BLOCK)\n    mask = offs < n_elements\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Contiguous flattening is valid for this elementwise op; if inputs are\n    # non-contiguous, make them contiguous so the 1D stream is coalesced.\n    a_c = a.contiguous()\n    b_c = b.contiguous()\n    c_c = c.contiguous()\n    o = torch.empty_like(a_c)\n\n    n_elements = a_c.numel()\n    BLOCK = 4096  # multiple of 64; large contiguous chunk to saturate HBM\n    grid = (triton.cdiv(n_elements, BLOCK),)\n    _add_add_relu_kernel[grid](a_c, b_c, c_c, o, n_elements,\n                               BLOCK=BLOCK, num_warps=8, num_stages=2)\n    return o.view_as(a)", "wall_us": 152.4, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, n_elements, BLOCK_N: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK_N + tl.arange(0, BLOCK_N)\n    mask = offs < n_elements\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    n_elements = a.numel()\n    BLOCK_N = 1024\n    grid = (triton.cdiv(n_elements, BLOCK_N),)\n    _add_add_relu_kernel[grid](a, b, c, o, n_elements, BLOCK_N=BLOCK_N, num_warps=4)\n    return o", "wall_us": 150.60000000000002, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, sa, sb, sc, so, N, BLOCK_N: tl.constexpr):\n    row = tl.program_id(0)\n    col = tl.program_id(1)\n    offs = col * BLOCK_N + tl.arange(0, BLOCK_N)\n    mask = offs < N\n    a = tl.load(a_ptr + row * sa + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + row * sb + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + row * sc + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + row * so + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    M, N = a.shape\n    o = torch.empty_like(a)\n    BLOCK_N = 1024\n    grid = (M, triton.cdiv(N, BLOCK_N))\n    _add_add_relu_kernel[grid](a, b, c, o, a.stride(0), b.stride(0), c.stride(0), o.stride(0), N,\n                       BLOCK_N=BLOCK_N, num_warps=8)\n    return o", "wall_us": 154.65, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, n_elements,\n                         BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    base = pid * BLOCK\n    offs = base + tl.arange(0, BLOCK)\n    mask = offs < n_elements\n\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Ensure contiguous so the whole tensor is a single 1D coalesced stream.\n    a_c = a.contiguous()\n    b_c = b.contiguous()\n    c_c = c.contiguous()\n    o = torch.empty_like(a_c)\n\n    n_elements = a_c.numel()\n\n    # Large flat blocks: one program handles a big contiguous burst of HBM,\n    # 64-lane wavefronts * 4 warps * 16 elems/lane => coalesced 4096-elem tile.\n    BLOCK = 4096\n    grid = (triton.cdiv(n_elements, BLOCK),)\n\n    _add_add_relu_kernel[grid](\n        a_c, b_c, c_c, o, n_elements,\n        BLOCK=BLOCK, num_warps=4, num_stages=2,\n    )\n    return o.view_as(a)", "wall_us": 153.5, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, n_elements, BLOCK_N: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK_N + tl.arange(0, BLOCK_N)\n    mask = offs < n_elements\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    n_elements = o.numel()\n    BLOCK_N = 1024\n    grid = (triton.cdiv(n_elements, BLOCK_N),)\n    _add_add_relu_kernel[grid](a, b, c, o, n_elements, BLOCK_N=BLOCK_N, num_warps=4)\n    return o", "wall_us": 153.79999999999998, "snr_db": 999.0, "rank": 5}], "preferences": [[0, 1], [0, 3], [0, 4], [0, 5], [2, 5]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "27bc0be4d93dc62a7134378842b8a37de2816d52dddc49ffbbd9e8d2458fb441", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    M, N = a.shape\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a, b, c, o, numel, BLOCK=BLOCK, num_warps=8)\n    return o", "wall_us": 154.4, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nMemory-bound: architectural change is a grid-stride loop with wide vectorized\nloads to maximize memory-level parallelism and HBM bandwidth on gfx942.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    num_programs = tl.num_programs(0)\n    # Each program strides across the array, processing UNROLL tiles per step.\n    # Grid-stride keeps many memory transactions in flight and amortizes launch cost.\n    base = pid * BLOCK\n    stride = num_programs * BLOCK\n\n    # UNROLL consecutive tiles handled per program iteration for wider MLP.\n    for u in tl.static_range(UNROLL):\n        offs = base + u * stride + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    M, N = a.shape\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 2048          # multiple of 64; smaller tile -> more VGPR headroom, wide loads\n    UNROLL = 4            # tiles processed per program (memory-level parallelism)\n\n    # Cap the grid so each program does real work (UNROLL tiles) -> grid-stride coverage.\n    total_tiles = triton.cdiv(numel, BLOCK)\n    grid_tiles = triton.cdiv(total_tiles, UNROLL)\n    grid = (grid_tiles,)\n\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL,\n        num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 154.29999999999998, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 1024  # multiple of 64 -> maps cleanly onto 64-lane wavefronts\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a, b, c, o, numel, BLOCK=BLOCK, num_warps=4)\n    return o", "wall_us": 152.2, "snr_db": 999.0, "rank": 0}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    M, N = a.shape\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 8192\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a, b, c, o, numel, BLOCK=BLOCK, num_warps=4)\n    return o", "wall_us": 154.4, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nMemory-bound: bottleneck is HBM bandwidth. Structural change: grid-stride\nvectorized loop so each program issues multiple wide, coalesced global loads,\nmaximizing memory-level parallelism and saturating bandwidth.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, VEC: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program handles VEC contiguous BLOCK-sized chunks, then strides.\n    base = pid * (BLOCK * VEC)\n    for v in tl.static_range(VEC):\n        offs = base + v * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    M, N = a.shape\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    # BLOCK is a multiple of 64 (maps to whole wavefronts). VEC gives each\n    # program several independent wide load streams -> more MLP per program,\n    # fewer program launches, better HBM saturation.\n    BLOCK = 2048\n    VEC = 4\n    grid = (triton.cdiv(numel, BLOCK * VEC),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, VEC=VEC,\n        num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 155.6, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a, b, c, o, numel, BLOCK=BLOCK, num_warps=4)\n    return o", "wall_us": 153.0, "snr_db": 999.0, "rank": 1}], "preferences": [[0, 1], [0, 3], [0, 4], [1, 4], [2, 1], [2, 3], [2, 4], [3, 1], [3, 4], [5, 1], [5, 3], [5, 4]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "75eaf005933ff0b67bdf2a98cb4019e07a68424020be642c55cd1ebef3d19a3d", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> more MLP / outstanding HBM loads per program\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a, b, c, o, numel, BLOCK=BLOCK, num_warps=4)\n    return o", "wall_us": 153.0, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = relu(a + b + c) computed in ONE pass.\nRestructured to a grid-stride loop to saturate HBM bandwidth on MI325X.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, TILES_PER_PROGRAM: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    num_programs = tl.num_programs(0)\n    # Each program strides across the array, processing TILES_PER_PROGRAM\n    # contiguous BLOCK-sized tiles per grid step. Grid-stride keeps the\n    # working set resident and amortizes launch/index overhead while the\n    # HBM controllers stay saturated.\n    base = pid * BLOCK\n    stride = num_programs * BLOCK\n\n    for _ in tl.static_range(TILES_PER_PROGRAM):\n        offs = base + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n        base += stride\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 2048              # multiple of 64 -> clean wavefront mapping\n    TILES_PER_PROGRAM = 4     # grid-stride tiles per program\n\n    elems_per_program = BLOCK * TILES_PER_PROGRAM\n    grid = (triton.cdiv(numel, elems_per_program),)\n\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK,\n        TILES_PER_PROGRAM=TILES_PER_PROGRAM,\n        num_warps=8,\n        num_stages=2,\n    )\n    return o", "wall_us": 155.0, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 1024  # multiple of 64 -> maps cleanly onto 64-lane wavefronts\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a, b, c, o, numel, BLOCK=BLOCK, num_warps=4)\n    return o", "wall_us": 152.5, "snr_db": 999.0, "rank": 0}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> more MLP per program, better HBM saturation\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a, b, c, o, numel, BLOCK=BLOCK, num_warps=4)\n    return o", "wall_us": 152.2, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = relu(a + b + c) computed in ONE pass.\nMemory-bound: restructured to process multiple contiguous elements per lane\n(vectorized strip mining) to maximize HBM bandwidth and load-issue efficiency\non MI325X (gfx942, 64-lane wavefronts).\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, VEC: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program handles BLOCK * VEC contiguous elements.\n    # Layout: VEC consecutive sub-tiles of size BLOCK, kept contiguous per program\n    # for coalesced, vectorized global loads/stores.\n    base = pid * (BLOCK * VEC)\n    lane = tl.arange(0, BLOCK)\n\n    for v in tl.static_range(VEC):\n        offs = base + v * BLOCK + lane\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0)\n        af = a.to(tl.float32)\n        bf = b.to(tl.float32)\n        cf = c.to(tl.float32)\n        o = tl.maximum(af + bf + cf, 0.0)\n        tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 1024   # multiple of 64 -> maps cleanly onto 64-lane wavefronts\n    VEC = 8        # elements-per-lane strip: fewer program launches, more BW/issue\n    tile = BLOCK * VEC\n    grid = (triton.cdiv(numel, tile),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, VEC=VEC,\n        num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 156.79999999999998, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(tl.float32), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty(a.shape, dtype=a.dtype, device=a.device)\n    numel = a.numel()\n    BLOCK = 1024  # multiple of 64 -> maps cleanly onto 64-lane wavefronts\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a, b, c, o, numel, BLOCK=BLOCK, num_warps=4)\n    return o", "wall_us": 152.5, "snr_db": 999.0, "rank": 2}], "preferences": [[0, 1], [0, 4], [1, 4], [2, 1], [2, 4], [3, 1], [3, 4], [5, 1], [5, 4]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "8bd94f3e93463d0f493636180614857369914d85105b59f07a487a8e492f4353", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> more elements/wavefront to hide HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a, b, c, o, numel, BLOCK=BLOCK, num_warps=4)\n    return o", "wall_us": 152.60000000000002, "snr_db": 999.0, "rank": 0}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program handles UNROLL contiguous BLOCK-wide tiles -> wider coalesced\n    # HBM transactions per wavefront and amortized launch/index overhead.\n    base = pid * (BLOCK * UNROLL)\n    for u in tl.static_range(UNROLL):\n        offs = base + u * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 1024   # multiple of 64 -> maps cleanly onto 64-lane wavefronts\n    UNROLL = 8     # each program processes 8 tiles = 8192 elements\n    grid = (triton.cdiv(numel, BLOCK * UNROLL),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL,\n        num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 158.4, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    orig_shape = a.shape\n    a = a.contiguous().view(-1)\n    b = b.contiguous().view(-1)\n    c = c.contiguous().view(-1)\n    numel = a.numel()\n    o = torch.empty(numel, dtype=a.dtype, device=a.device)\n    BLOCK = 1024  # multiple of 64 -> maps cleanly onto 64-lane wavefronts\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a, b, c, o, numel, BLOCK=BLOCK, num_warps=4)\n    return o.view(orig_shape)", "wall_us": 152.29999999999998, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> more elements/program to hide HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a, b, c, o, numel, BLOCK=BLOCK, num_warps=4)\n    return o", "wall_us": 151.0, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRestructured to a grid-stride, vectorized loop to saturate HBM bandwidth on MI325X.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, VEC: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    num_programs = tl.num_programs(0)\n\n    # Each program handles a tile of BLOCK*VEC elements, then strides forward.\n    tile = BLOCK * VEC\n    grid_stride = num_programs * tile\n\n    base = pid * tile\n    lane = tl.arange(0, BLOCK)\n\n    # Grid-stride outer loop: keeps program count fixed, work scales with numel.\n    for start in range(base, numel, grid_stride):\n        # VEC-way unrolled inner block -> wide, coalesced, many in-flight loads.\n        for v in tl.static_range(VEC):\n            offs = start + v * BLOCK + lane\n            mask = offs < numel\n            a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            o = tl.maximum(a + b + c, 0.0)\n            tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 1024      # multiple of 64 -> maps cleanly onto 64-lane wavefronts\n    VEC = 4           # per-program unroll -> more memory-level parallelism\n    tile = BLOCK * VEC\n\n    # Fixed, occupancy-friendly program count; grid-stride covers the rest.\n    # Cap programs so each does real work but we still fill the machine.\n    max_programs = triton.cdiv(numel, tile)\n    grid = (min(max_programs, 65536),)\n\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, VEC=VEC,\n        num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 154.29999999999998, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Preserve original output shape, but operate on flat contiguous views so\n    # numel and linear indexing stay consistent across all three inputs.\n    out_shape = a.shape\n    a_flat = a.contiguous().reshape(-1)\n    b_flat = b.contiguous().reshape(-1)\n    c_flat = c.contiguous().reshape(-1)\n\n    numel = a_flat.numel()\n    o_flat = torch.empty_like(a_flat)\n\n    BLOCK = 1024  # multiple of 64 -> maps cleanly onto 64-lane wavefronts\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4)\n    return o_flat.reshape(out_shape)", "wall_us": 152.5, "snr_db": 999.0, "rank": 3}], "preferences": [[0, 1], [0, 4], [2, 1], [3, 1], [3, 4], [4, 1], [5, 1]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "7199307013a50229258fa6d2d50a1fc7adb7afa3f7c6d581dd1f952315f27ea8", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 8192  # multiple of 64 -> deeper in-flight load window to saturate HBM\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a, b, c, o, numel, BLOCK=BLOCK, num_warps=4)\n    return o", "wall_us": 154.1, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nMemory-bound: 3 HBM reads + 1 HBM write per element, no reuse -> pure bandwidth play.\n\nArchitectural change: each program now processes VEC contiguous chunks of BLOCK\nelements via an unrolled inner loop. This keeps many independent global loads\nin flight per wavefront (more memory-level parallelism), lets the compiler\ncoalesce into wide HBM bursts, and reduces launch/index overhead per element.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, VEC: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program owns VEC consecutive BLOCK-sized tiles -> big contiguous span.\n    base = pid * (BLOCK * VEC)\n    lane = tl.arange(0, BLOCK)\n\n    # Unrolled inner loop: issue many independent loads before consuming them,\n    # maximizing outstanding HBM requests per wavefront (memory-level parallelism).\n    for v in tl.static_range(VEC):\n        offs = base + v * BLOCK + lane\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 2048   # multiple of 64; per-tile lane width\n    VEC = 8        # tiles processed per program -> 16384 elems/program\n    grid = (triton.cdiv(numel, BLOCK * VEC),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, VEC=VEC, num_warps=8, num_stages=2,\n    )\n    return o", "wall_us": 157.4, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(o_ptr.dtype.element_ty), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> more elements/wavefront to hide HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a, b, c, o, numel, BLOCK=BLOCK, num_warps=4)\n    return o", "wall_us": 153.2, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> more elements/wavefront to hide HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 150.79999999999998, "snr_db": 999.0, "rank": 0}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\n\nEXPLORE change: grid-strided loop with per-program unrolling (UNROLL chunks).\nEach wavefront issues UNROLL independent load groups per iteration, maximizing\nmemory-level parallelism to saturate MI325X HBM3 bandwidth. This decouples the\nlaunch grid size from numel and gives the scheduler many outstanding HBM\ntransactions, which the single-load-per-program scheme could not.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr, GRID: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Total elements handled per program per loop iteration.\n    step = BLOCK * UNROLL\n    # Grid-strided base so all programs stay resident and stream memory.\n    stride = GRID * step\n\n    base = pid * step\n    while base < numel:\n        # Unrolled, independent load/compute/store groups -> many in-flight\n        # HBM transactions per wavefront (memory-level parallelism).\n        for u in tl.static_range(UNROLL):\n            offs = base + u * BLOCK + tl.arange(0, BLOCK)\n            mask = offs < numel\n            a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            o = tl.maximum(a + b + c, 0.0)\n            tl.store(o_ptr + offs, o, mask=mask)\n        base += stride\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 2048    # multiple of 64; per-load tile size\n    UNROLL = 4      # independent load groups per program -> memory-level parallelism\n\n    # Cap the grid so each program does real strided work (persistent-style).\n    # ~2 waves per CU across 304 CUs is plenty to saturate HBM for a streaming op.\n    MAX_PROGRAMS = 4096\n    needed = triton.cdiv(numel, BLOCK * UNROLL)\n    GRID = min(MAX_PROGRAMS, max(1, needed))\n\n    grid = (GRID,)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL, GRID=GRID,\n        num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 154.70000000000002, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 1024  # multiple of 64; smaller tile lowers register pressure for correctness\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](a, b, c, o, numel, BLOCK=BLOCK, num_warps=4)\n    return o", "wall_us": 153.6, "snr_db": 999.0, "rank": 1}], "preferences": [[0, 1], [0, 4], [2, 1], [2, 4], [3, 0], [3, 1], [3, 4], [4, 1], [5, 0], [5, 1], [5, 4]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "09a554b32d07210c504b51c676d0dba6db3b700cc6f1c81e28172da115a4fc8d", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> more elements/wavefront to hide HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o", "wall_us": 152.5, "snr_db": 999.0, "rank": 0}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nMemory-bound: bandwidth-limited by 3 loads + 1 store per element.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program owns UNROLL contiguous BLOCK-sized chunks. This lets the\n    # compiler pipeline several independent wide (dwordx4) global loads,\n    # boosting memory-level parallelism to saturate HBM bandwidth.\n    base = pid * (BLOCK * UNROLL)\n    for u in tl.static_range(UNROLL):\n        offs = base + u * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 2048   # multiple of 64; sized for wide vectorized loads per wavefront\n    UNROLL = 4     # independent chunks per program -> deeper load pipeline (MLP)\n    tile = BLOCK * UNROLL\n    grid = (triton.cdiv(numel, tile),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL,\n        num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 152.9, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(o_ptr.dtype.element_ty), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> more elements/wavefront to hide HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 153.6, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> more elements/wavefront to hide HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o", "wall_us": 152.5, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRestructured to a grid-stride, vectorized (multi-element-per-lane) loop to better\nsaturate HBM bandwidth on MI325X (gfx942).\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr,          # elements processed per program per step (mult of 64)\n    UNROLL: tl.constexpr,         # number of BLOCK-sized sub-tiles handled per step\n):\n    pid = tl.program_id(0)\n    num_programs = tl.num_programs(0)\n\n    # Each program handles UNROLL contiguous sub-tiles per iteration, then strides\n    # forward by the whole grid's footprint. This keeps many wide loads in flight\n    # (LDS double-buffering via num_stages) to hide HBM latency.\n    step = BLOCK * UNROLL * num_programs\n    base = pid * BLOCK * UNROLL\n\n    for start in tl.range(base, numel, step):\n        for u in tl.static_range(UNROLL):\n            offs = start + u * BLOCK + tl.arange(0, BLOCK)\n            mask = offs < numel\n            a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            o = tl.maximum(a + b + c, 0.0)\n            tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 2048    # elements per sub-tile (multiple of 64)\n    UNROLL = 4      # sub-tiles per program per grid-stride step\n\n    # Launch a fixed, occupancy-friendly grid and let each program stride over the\n    # array. This decouples grid size from numel and keeps CUs fully populated.\n    tile = BLOCK * UNROLL\n    max_programs = triton.cdiv(numel, tile)\n    # Cap grid to a healthy multiple of the CU count so each program does real work\n    # while still exposing enough parallelism for the scheduler.\n    grid_cap = 4096\n    grid = (min(max_programs, grid_cap),)\n\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL,\n        num_warps=4, num_stages=3,\n    )\n    return o", "wall_us": 154.9, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(o_ptr.dtype.element_ty), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> more elements/wavefront to hide HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 153.6, "snr_db": 999.0, "rank": 3}], "preferences": [[0, 1], [0, 2], [0, 4], [0, 5], [2, 1], [2, 4], [3, 1], [3, 2], [3, 4], [3, 5], [5, 1], [5, 4]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "5b57cb08debc223171896d2dd7118477adac3b3fa9ce3116cfff75875c633d54", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> more elements/wavefront to hide HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=8, num_stages=4\n    )\n    return o", "wall_us": 153.10000000000002, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nMemory-bound: 3 loads + 1 store per element. Architectural change here is a\ngrid-stride loop so a bounded number of long-lived workgroups keep many HBM\nrequests outstanding, improving effective bandwidth on gfx942/MI325X.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, TILES_PER_PROGRAM: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    num_programs = tl.num_programs(0)\n    # Stride (in elements) between consecutive tiles handled by one program's\n    # neighbors; a full sweep of all programs covers `stride` elements.\n    stride = num_programs * BLOCK\n    base = pid * BLOCK\n    # Each program processes TILES_PER_PROGRAM tiles spaced `stride` apart.\n    # This grid-stride pattern keeps workgroups alive longer, sustaining more\n    # in-flight HBM transactions than a one-tile-per-program launch.\n    for t in tl.static_range(TILES_PER_PROGRAM):\n        offs = base + t * stride + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 4096            # multiple of 64 -> wide vectorized tiles\n    TILES_PER_PROGRAM = 4   # grid-stride: each program sweeps several tiles\n\n    total_tiles = triton.cdiv(numel, BLOCK)\n    grid = (triton.cdiv(total_tiles, TILES_PER_PROGRAM),)\n\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, TILES_PER_PROGRAM=TILES_PER_PROGRAM,\n        num_warps=8, num_stages=2,\n    )\n    return o", "wall_us": 156.0, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty(a.shape, dtype=a.dtype, device=a.device).contiguous()\n    numel = a.numel()\n    BLOCK = 1024  # multiple of 64; keeps VGPR pressure low and avoids compiler limits\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 152.0, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 8192  # multiple of 64 -> wider coalesced loads, more MLP to hide HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o", "wall_us": 150.79999999999998, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, ITERS: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    num_programs = tl.num_programs(0)\n    # Grid-stride: each program sweeps ITERS chunks strided across the whole grid,\n    # keeping many independent HBM loads in flight per wavefront to hide latency.\n    stride = num_programs * BLOCK\n    base = pid * BLOCK\n    for i in tl.static_range(ITERS):\n        offs = base + i * stride + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 4096          # multiple of 64 -> full wavefronts, coalesced HBM access\n    ITERS = 4             # each program handles ITERS chunks -> more in-flight loads\n\n    # Size the grid so total programs * BLOCK * ITERS covers numel, but cap the grid\n    # to keep enough work per program for grid-stride latency hiding.\n    total_blocks = triton.cdiv(numel, BLOCK)\n    grid_blocks = triton.cdiv(total_blocks, ITERS)\n    grid = (grid_blocks,)\n\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, ITERS=ITERS,\n        num_warps=8, num_stages=2,\n    )\n    return o", "wall_us": 154.0, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 1024  # multiple of 64 -> maps cleanly onto 64-lane wavefronts, avoids spills\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 151.79999999999998, "snr_db": 999.0, "rank": 0}], "preferences": [[0, 1], [0, 4], [2, 1], [2, 4], [3, 1], [3, 4], [5, 1], [5, 4]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "cb22398d3f51bff4e25b2ff456e0bb63a41c340af314317f842b8baa4c261ae0", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> more in-flight loads per program to hide HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 152.79999999999998, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nGrid-stride streamed variant: a fixed pool of programs each walks the array in\nBLOCK-sized strides, keeping many coalesced memory transactions in flight to\nsaturate HBM bandwidth (this op is purely memory-bound, no MFMA needed).\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr,\n    UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    num_pid = tl.num_programs(0)\n    # Each program processes UNROLL independent BLOCK-wide tiles per step,\n    # then strides forward by the whole grid's footprint. Multiple in-flight\n    # loads per iteration keep HBM queues busy on bandwidth-bound work.\n    tiles_per_step = BLOCK * UNROLL\n    step = num_pid * tiles_per_step\n    base = pid * tiles_per_step\n\n    lane = tl.arange(0, BLOCK)\n\n    for start in range(base, numel, step):\n        for u in tl.static_range(UNROLL):\n            offs = start + u * BLOCK + lane\n            mask = offs < numel\n            a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            o = tl.maximum(a + b + c, 0.0)\n            tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 1024           # multiple of 64 -> clean wavefront mapping, wide coalesced loads\n    UNROLL = 4             # multiple in-flight tiles per program to fill memory queues\n\n    tiles_per_step = BLOCK * UNROLL\n    n_tiles = triton.cdiv(numel, tiles_per_step)\n\n    # Fixed-size program pool: enough to fully occupy the 304 CUs of MI325X,\n    # but capped so each program does real streamed work (grid-stride loop).\n    MAX_PROGRAMS = 8192\n    grid_size = min(n_tiles, MAX_PROGRAMS)\n    grid_size = max(grid_size, 1)\n    grid = (grid_size,)\n\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL,\n        num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 156.20000000000002, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1-D contiguous views so linear indexing over numel is valid\n    # for all three operands regardless of original layout/shape.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a)\n    o_flat = o.view(-1)\n\n    numel = a_flat.numel()\n    BLOCK = 1024  # multiple of 64 -> maps cleanly onto 64-lane wavefronts, avoids spills\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 153.4, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> larger per-wave work amortizes HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 152.10000000000002, "snr_db": 999.0, "rank": 0}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nMemory-bound: optimize for HBM bandwidth via wide per-program tiles + vectorized loads.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program processes UNROLL contiguous sub-tiles of size BLOCK.\n    base = pid * (BLOCK * UNROLL)\n    for u in tl.static_range(UNROLL):\n        offs = base + u * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    # Wide vectorized tiling: each program handles BLOCK*UNROLL elements.\n    # BLOCK is a multiple of 64 (maps to 64-lane wavefronts / MFMA-friendly),\n    # UNROLL boosts memory-level parallelism and cuts grid launch overhead.\n    BLOCK = 1024          # multiple of 64\n    UNROLL = 8            # 8K elements per program -> deep in-flight load pipeline\n    tile = BLOCK * UNROLL\n    grid = (triton.cdiv(numel, tile),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL,\n        num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 156.6, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty(a.shape, dtype=torch.float32, device=a.device)\n    numel = a.numel()\n    BLOCK = 1024  # multiple of 64 -> maps cleanly onto 64-lane wavefronts, avoids spills\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 151.79999999999998, "snr_db": 999.0, "rank": 3}], "preferences": [[0, 1], [0, 4], [1, 4], [2, 1], [2, 4], [3, 1], [3, 4], [5, 4]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "b5eb9d13a956f87ed7fdc71f2eb6d8964595a6d23f19f60e519acc94b6acb777", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> larger per-wave work amortizes HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o", "wall_us": 152.4, "snr_db": 999.0, "rank": 0}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    # Each program processes UNROLL contiguous BLOCK-sized tiles.\n    # This grid-stride style unroll raises memory-level parallelism per\n    # wavefront: multiple wide loads can be in-flight before any store,\n    # better hiding HBM latency on a bandwidth-bound pointwise op.\n    pid = tl.program_id(0)\n    base = pid * (BLOCK * UNROLL)\n    for u in tl.static_range(UNROLL):\n        offs = base + u * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 2048      # multiple of 64; wide per-tile load\n    UNROLL = 4        # tiles processed per program -> higher MLP\n    tile = BLOCK * UNROLL\n    grid = (triton.cdiv(numel, tile),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL,\n        num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 156.1, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n\n    a_flat = a.view(-1)\n    b_flat = b.view(-1)\n    c_flat = c.view(-1)\n    o_flat = o.view(-1)\n\n    numel = a_flat.numel()\n    BLOCK = 4096  # multiple of 64 -> larger per-wave work amortizes HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 153.25, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> larger per-wave work amortizes HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o", "wall_us": 152.4, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    num_programs = tl.num_programs(0)\n    # Each program strides across the array, processing UNROLL tiles per hop.\n    # This amortizes launch overhead and keeps many memory ops in flight\n    # per wavefront (better HBM latency hiding on CDNA3).\n    step = BLOCK * num_programs\n    base = pid * BLOCK\n\n    for u in tl.static_range(UNROLL):\n        offs = base + u * step + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 2048   # multiple of 64 -> clean wavefront/MFMA-lane mapping\n    UNROLL = 4     # tiles processed per program via grid-stride loop\n    elems_per_program = BLOCK * UNROLL\n    grid = (triton.cdiv(numel, elems_per_program),)\n\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL,\n        num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 154.79999999999998, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a, dtype=torch.float32)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> larger per-wave work amortizes HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 152.10000000000002, "snr_db": 999.0, "rank": 2}], "preferences": [[0, 1], [0, 4], [1, 4], [2, 1], [2, 4], [3, 1], [3, 4], [5, 1], [5, 4]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "e9803fa65836e2f67f00dac63df808757f4d50ea6bb5080a891d4527e19ae132", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    base = pid * BLOCK * UNROLL\n    # Each program handles UNROLL independent BLOCK-sized chunks so multiple\n    # global loads are in flight simultaneously -> better HBM latency hiding.\n    for u in tl.static_range(UNROLL):\n        offs = base + u * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096   # multiple of 64 -> larger per-wave work amortizes HBM latency\n    UNROLL = 4     # independent chunks per program to increase in-flight loads\n    grid = (triton.cdiv(numel, BLOCK * UNROLL),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, UNROLL=UNROLL, num_warps=8, num_stages=2\n    )\n    return o", "wall_us": 158.4, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nMemory-bound: 3 loads + 1 store per element. Optimized for HBM bandwidth on MI325X.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program processes UNROLL contiguous BLOCK-wide strips.\n    base = pid * BLOCK * UNROLL\n    for u in tl.static_range(UNROLL):\n        offs = base + u * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0)\n        o = tl.maximum(a.to(tl.float32) + b.to(tl.float32) + c.to(tl.float32), 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    # Wavefront = 64. BLOCK is a multiple of 64; UNROLL gives each program\n    # several coalesced strips to amortize launch/address overhead and keep\n    # the HBM read pipeline saturated (software-pipelined by num_stages).\n    BLOCK = 2048          # multiple of 64\n    UNROLL = 4            # elements per program = BLOCK * UNROLL = 8192\n    elems_per_program = BLOCK * UNROLL\n    grid = (triton.cdiv(numel, elems_per_program),)\n\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL,\n        num_warps=8, num_stages=2,\n    )\n    return o", "wall_us": 155.6, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    # Store back in the output tensor's native element dtype to avoid dtype mismatch.\n    o = o.to(o_ptr.dtype.element_ty)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> larger per-wave work amortizes HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o", "wall_us": 153.70000000000002, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> larger per-wave work amortizes HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=8, num_stages=4\n    )\n    return o", "wall_us": 151.79999999999998, "snr_db": 999.0, "rank": 0}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nStructural change: each program handles multiple contiguous sub-blocks so the\nbackend coalesces into wide (dwordx4) global loads, saturating HBM bandwidth.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program owns UNROLL contiguous sub-blocks of size BLOCK.\n    base = pid * BLOCK * UNROLL\n    for u in tl.static_range(UNROLL):\n        offs = base + u * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 2048   # multiple of 64; each lane pulls a fat, coalesced strip\n    UNROLL = 8     # 8 contiguous sub-blocks per program -> deep load pipeline\n    grid = (triton.cdiv(numel, BLOCK * UNROLL),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL,\n        num_warps=8, num_stages=2,\n    )\n    return o", "wall_us": 158.0, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty(a.shape, dtype=a.dtype, device=a.device).contiguous()\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> larger per-wave work amortizes HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o", "wall_us": 152.4, "snr_db": 999.0, "rank": 2}], "preferences": [[0, 4], [1, 0], [1, 4], [2, 0], [2, 1], [2, 4], [3, 0], [3, 1], [3, 4], [5, 0], [5, 1], [5, 4]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "e8c072a70442324900143a057c6c34dc3057f131084ecbf55fe9cdb22d43df4c", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 8192  # multiple of 64 -> more in-flight HBM loads per wave, better latency hiding\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=8, num_stages=4\n    )\n    return o", "wall_us": 153.4, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nMemory-bound: 3 loads + 1 store per element. Restructured to a grid-stride\nvectorized loop so each program keeps many HBM loads in flight, saturating\nbandwidth and amortizing launch overhead on MI325X (gfx942, wave=64).\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    num_programs = tl.num_programs(0)\n    # Each iteration handles BLOCK * UNROLL contiguous elements.\n    chunk = BLOCK * UNROLL\n    # Grid-stride: this program strides over the whole tensor in `chunk` steps.\n    stride = num_programs * chunk\n    base = pid * chunk\n\n    lane = tl.arange(0, BLOCK)\n    for start in range(base, numel, stride):\n        # Unroll UNROLL independent BLOCK-wide tiles to keep loads in flight.\n        for u in tl.static_range(UNROLL):\n            offs = start + u * BLOCK + lane\n            mask = offs < numel\n            a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            o = tl.maximum(a + b + c, 0.0)\n            tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 1024        # multiple of 64 -> 16 wavefronts per tile\n    UNROLL = 4          # independent tiles in flight per program\n\n    # Persistent-style grid: cap program count so each program does a grid-stride\n    # loop, keeping many HBM transactions outstanding to hide latency.\n    chunk = BLOCK * UNROLL\n    max_programs = triton.cdiv(numel, chunk)\n    # Target ~ enough programs to fill the GPU without oversubscription.\n    grid_cap = 4096\n    grid = (min(max_programs, grid_cap),)\n\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL,\n        num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 152.7, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64 -> larger per-wave work amortizes HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o", "wall_us": 152.4, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 8192  # multiple of 64 -> more in-flight loads per wave better amortize HBM latency\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=8, num_stages=4\n    )\n    return o", "wall_us": 152.79999999999998, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, ITERS: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    num_pid = tl.num_programs(0)\n    # Grid-stride: each program strides across the array in BLOCK-sized chunks.\n    # A fixed, modest grid keeps many memory transactions in flight per CU and\n    # amortizes per-tile overhead, saturating HBM bandwidth for this\n    # memory-bound pointwise fusion.\n    stride = num_pid * BLOCK\n    base = pid * BLOCK\n    for i in tl.static_range(ITERS):\n        offs = base + i * stride + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 4096  # multiple of 64 -> full-wave vectorized loads\n    # Fixed launch grid: ~MI325X has 304 CUs; oversubscribe modestly so each\n    # program does a grid-stride loop rather than spawning millions of tiny ones.\n    GRID = 2048\n    total_tiles = triton.cdiv(numel, BLOCK)\n    ITERS = max(1, triton.cdiv(total_tiles, GRID))\n    grid = (GRID,)\n\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, ITERS=ITERS,\n        num_warps=8, num_stages=2,\n    )\n    return o", "wall_us": 155.20000000000002, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 1024  # multiple of 64; smaller tile avoids pipeline/LDS resource limits\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 152.10000000000002, "snr_db": 999.0, "rank": 0}], "preferences": [[0, 4], [1, 4], [2, 4], [3, 4], [5, 4]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "331f05ebf7ad52fc7ef1cd07782105ee7d13dfca3bed362039c5a1d07704c3a0", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    # Memory-bound pointwise: larger tile gives more in-flight loads per program,\n    # better latency hiding and fewer launches. 4096 = 64*64, multiple of 64.\n    BLOCK = 4096\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 152.7, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nMemory-bound: restructured to a vectorized strip-per-program scheme so each program\nprocesses several contiguous BLOCK-wide tiles, widening HBM transactions and\nshrinking the launch grid for better memory-level parallelism on MI325X.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program handles UNROLL contiguous BLOCK-wide tiles.\n    base = pid * (BLOCK * UNROLL)\n    for u in tl.static_range(UNROLL):\n        offs = base + u * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 1024   # multiple of 64; one wide coalesced transaction per tile\n    UNROLL = 8     # tiles per program -> fewer, fatter programs, more MLP per wave\n    elems_per_prog = BLOCK * UNROLL\n\n    grid = (triton.cdiv(numel, elems_per_prog),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL,\n        num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 157.4, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o.to(o_ptr.dtype.element_ty), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 1024  # multiple of 64; smaller tile avoids pipeline/LDS resource limits\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 152.29999999999998, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n    BLOCK = 4096  # multiple of 64; larger tile boosts memory-level parallelism on bandwidth-bound op\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o", "wall_us": 152.29999999999998, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, ITERS: tl.constexpr,\n):\n    # Grid-stride loop: each program handles ITERS tiles of BLOCK elements.\n    # This shrinks the grid, cuts launch overhead, and lets the compiler\n    # emit wide coalesced vector loads while keeping HBM saturated.\n    pid = tl.program_id(0)\n    num_programs = tl.num_programs(0)\n    stride = BLOCK * num_programs\n\n    base = pid * BLOCK + tl.arange(0, BLOCK)\n    for i in tl.static_range(ITERS):\n        offs = base + i * stride\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    a = a.contiguous()\n    b = b.contiguous()\n    c = c.contiguous()\n    o = torch.empty_like(a)\n    numel = a.numel()\n\n    BLOCK = 1024          # multiple of 64; wavefront-friendly tile\n    ITERS = 8             # tiles processed per program via grid-stride loop\n\n    # Cap grid so each program does real work; keeps launch overhead low\n    # while ensuring enough blocks to fully occupy all CUs on MI325X.\n    total_tiles = triton.cdiv(numel, BLOCK)\n    grid_size = triton.cdiv(total_tiles, ITERS)\n    grid = (max(grid_size, 1),)\n\n    _add_add_relu_kernel[grid](\n        a, b, c, o, numel,\n        BLOCK=BLOCK, ITERS=ITERS,\n        num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 155.5, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    BLOCK = 1024  # multiple of 64; smaller tile avoids pipeline/LDS resource limits\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o.view_as(a)", "wall_us": 152.9, "snr_db": 999.0, "rank": 0}], "preferences": [[0, 1], [0, 4], [2, 1], [2, 4], [3, 1], [3, 4], [5, 1], [5, 4]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "fbf1302df03786f4a86535bdb245260b69e773a7b9390cfef0deed9eb78febad", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    # Memory-bound pointwise op: larger tile -> more in-flight global loads per\n    # wavefront, deeper coalescing, better HBM bandwidth utilization / latency hiding.\n    BLOCK = 4096  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o.view_as(a)", "wall_us": 150.4, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nMemory-bound: bounded by HBM bandwidth. Uses a grid-stride loop with a wide\nper-program tile so each wavefront keeps many independent loads in flight,\nsaturating HBM with fewer, fatter program launches.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    num_programs = tl.num_programs(0)\n    # Each program owns BLOCK*UNROLL elements per grid-stride step.\n    tile = BLOCK * UNROLL\n    stride = num_programs * tile\n    base = pid * tile\n\n    # Grid-stride: loop over the flattened array in fat chunks.\n    for start in range(base, numel, stride):\n        for u in tl.static_range(UNROLL):\n            offs = start + u * BLOCK + tl.arange(0, BLOCK)\n            mask = offs < numel\n            a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            o = tl.maximum(a + b + c, 0.0)\n            tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    # Wide per-program footprint: BLOCK (multiple of 64) times UNROLL independent\n    # loads keeps many outstanding memory transactions per wavefront. A fixed,\n    # bounded grid lets each program stride across the array, so launch overhead\n    # is amortized and HBM stays busy.\n    BLOCK = 1024          # multiple of 64\n    UNROLL = 4            # independent sub-tiles issued back-to-back\n    tile = BLOCK * UNROLL\n\n    # Cap the grid so programs actually iterate (better MLP), but keep enough\n    # blocks to fill all CUs on the MI325X.\n    n_tiles = triton.cdiv(numel, tile)\n    MAX_PROGRAMS = 8192\n    grid = (min(n_tiles, MAX_PROGRAMS),)\n\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL,\n        num_warps=4, num_stages=2,\n    )\n    return o.view_as(a)", "wall_us": 155.5, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    BLOCK = 1024  # multiple of 64; smaller tile avoids pipeline/LDS resource limits\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    # Robust reshape back to the original logical shape (handles any contiguity).\n    return o.reshape(a.shape)", "wall_us": 151.60000000000002, "snr_db": 999.0, "rank": 0}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    # Memory-bound pointwise: widen the tile so each program issues fatter,\n    # more coalesced HBM loads and hides latency with more in-flight requests.\n    BLOCK = 4096  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o.view_as(a)", "wall_us": 149.9, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nStructural change: grid-stride loop with multiple elements processed per program to\nmaximize in-flight HBM loads (memory-level parallelism) on the MI325X.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program owns UNROLL contiguous tiles of BLOCK elements.\n    base = pid * (BLOCK * UNROLL)\n    for u in tl.static_range(UNROLL):\n        offs = base + u * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    # BLOCK is a multiple of 64 (maps to whole wavefronts / MFMA lanes).\n    # UNROLL lets each program issue several independent load groups, exposing\n    # more memory-level parallelism and amortizing launch overhead \u2014 the key\n    # structural lever for a bandwidth-bound pointwise op.\n    BLOCK = 1024\n    UNROLL = 4\n    tile = BLOCK * UNROLL\n    grid = (triton.cdiv(numel, tile),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL, num_warps=4, num_stages=2,\n    )\n    return o.view_as(a)", "wall_us": 154.1, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    BLOCK = 1024  # multiple of 64; smaller tile avoids pipeline/LDS resource limits\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    # Reshape to original logical shape (avoids view_as failing on non-contiguous input).\n    return o.reshape(a.shape)", "wall_us": 152.4, "snr_db": 999.0, "rank": 2}], "preferences": [[0, 1], [2, 1], [2, 4], [5, 1]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "136af383aca1c7f204352ec8a605c6164d7ec022d38416c736da3a5569f2c31a", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    # Memory-bound pointwise op: larger tile lets each wavefront keep more\n    # HBM loads in flight, improving memory-level parallelism / latency hiding.\n    BLOCK = 4096  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    # Robust reshape back to the original logical shape (handles any contiguity).\n    return o.reshape(a.shape)", "wall_us": 151.79999999999998, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nMemory-bandwidth bound: each program processes several BLOCK-wide chunks so global\nloads coalesce into wide transactions and expose more memory-level parallelism (MLP),\nraising achieved HBM bandwidth on gfx942.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program owns UNROLL contiguous BLOCK-wide tiles.\n    base = pid * (BLOCK * UNROLL)\n    for i in tl.static_range(UNROLL):\n        offs = base + i * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    # BLOCK multiple of 64; UNROLL raises work-per-program to amortize launch and\n    # boost MLP. Effective tile per program = BLOCK * UNROLL.\n    BLOCK = 1024\n    UNROLL = 4\n    tile = BLOCK * UNROLL\n    grid = (triton.cdiv(numel, tile),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL, num_warps=4, num_stages=2,\n    )\n    # Robust reshape back to the original logical shape (handles any contiguity).\n    return o.reshape(a.shape)", "wall_us": 152.5, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().reshape(-1)\n    b_flat = b.contiguous().reshape(-1)\n    c_flat = c.contiguous().reshape(-1)\n\n    numel = a_flat.numel()\n    # Allocate flat output explicitly in fp32 (matches input dtype for this fp32 task).\n    o_flat = torch.empty(numel, dtype=a_flat.dtype, device=a_flat.device)\n\n    BLOCK = 1024  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    # Reshape back to the original logical shape.\n    return o_flat.reshape(a.shape)", "wall_us": 152.2, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    # Memory-bound pointwise kernel: enlarge tile to expose more in-flight loads\n    # per wavefront and better hide HBM latency (multiple of 64).\n    BLOCK = 4096\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    # Robust reshape back to the original logical shape (handles any contiguity).\n    return o.reshape(a.shape)", "wall_us": 152.7, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nStructural change: grid-stride loop with large per-program footprint so each\nwavefront keeps many wide HBM transactions in flight (memory-bound saturation).\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    num_programs = tl.num_programs(0)\n    # Each program processes UNROLL contiguous BLOCK-tiles, then strides by the\n    # whole grid. This raises memory-level parallelism per wavefront while\n    # keeping loads perfectly coalesced within each tile.\n    tile = pid * BLOCK\n    stride = num_programs * BLOCK * UNROLL\n\n    base = tile\n    while base < numel:\n        for u in tl.static_range(UNROLL):\n            offs = base + u * (num_programs * BLOCK) + tl.arange(0, BLOCK)\n            mask = offs < numel\n            a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n            o = tl.maximum(a + b + c, 0.0)\n            tl.store(o_ptr + offs, o, mask=mask)\n        base += stride\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    BLOCK = 1024      # multiple of 64; coalesced tile per wavefront-group\n    UNROLL = 8        # tiles processed per program before striding -> high MLP\n\n    # Launch a fixed, occupancy-friendly grid sized to the device rather than to\n    # numel; the grid-stride loop then sweeps arbitrarily large tensors.\n    total_tiles = triton.cdiv(numel, BLOCK)\n    max_programs = triton.cdiv(total_tiles, UNROLL)\n    # Cap the grid so each program gets meaningful work (MLP), but never exceed\n    # the number of tiles present.\n    grid_size = min(max_programs, 65536)\n    grid_size = max(grid_size, 1)\n    grid = (grid_size,)\n\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL,\n        num_warps=4, num_stages=2,\n    )\n    # Robust reshape back to the original logical shape (handles any contiguity).\n    return o.reshape(a.shape)", "wall_us": 155.79999999999998, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    # Guard against empty tensors: nothing to launch, just reshape.\n    if numel == 0:\n        return o.reshape(a.shape)\n\n    BLOCK = 1024  # multiple of 64; smaller tile avoids pipeline/LDS resource limits\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    # Robust reshape back to the original logical shape (handles any contiguity).\n    return o.reshape(a.shape)", "wall_us": 150.0, "snr_db": 999.0, "rank": 0}], "preferences": [[0, 1], [0, 4], [1, 4], [2, 4], [3, 4], [5, 1], [5, 4]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "3261d0f574f8a0b0e757276aa7ccec168374904f6caf4de5d68f772c7e613fa3", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    # Guard against empty tensors: nothing to launch, just reshape.\n    if numel == 0:\n        return o.reshape(a.shape)\n\n    # Larger tile -> more in-flight loads per wavefront to hide HBM latency\n    # (memory-bound kernel). 4096 is a multiple of 64.\n    BLOCK = 4096\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    # Robust reshape back to the original logical shape (handles any contiguity).\n    return o.reshape(a.shape)", "wall_us": 153.0, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nStructural change: persistent grid-stride loop so a small number of long-lived\nprograms stream many contiguous BLOCK chunks, saturating HBM bandwidth.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, NUM_PROGRAMS: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Persistent grid-stride: each program walks multiple BLOCK-sized chunks.\n    stride = NUM_PROGRAMS * BLOCK\n    base = pid * BLOCK\n    lane = tl.arange(0, BLOCK)\n\n    # Grid-stride loop over the flattened array. Long-lived programs keep the\n    # memory pipeline full and amortize launch overhead.\n    for start in range(base, numel, stride):\n        offs = start + lane\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    # Guard against empty tensors: nothing to launch, just reshape.\n    if numel == 0:\n        return o.reshape(a.shape)\n\n    BLOCK = 2048  # multiple of 64; larger contiguous chunk per iteration\n\n    # Persistent launch: cap the number of programs so each is long-lived and\n    # streams many chunks via the grid-stride loop. Size it to a few waves of\n    # the MI325X's ~304 CUs rather than one program per tile.\n    n_tiles = triton.cdiv(numel, BLOCK)\n    NUM_PROGRAMS = min(n_tiles, 304 * 8)  # a few full waves of CUs\n    grid = (NUM_PROGRAMS,)\n\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel,\n        BLOCK=BLOCK, NUM_PROGRAMS=NUM_PROGRAMS,\n        num_warps=4, num_stages=2,\n    )\n    # Robust reshape back to the original logical shape (handles any contiguity).\n    return o.reshape(a.shape)", "wall_us": 153.2, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    # Guard against empty tensors: nothing to launch, just reshape.\n    if numel == 0:\n        return o.reshape(a.shape)\n\n    BLOCK = 1024  # multiple of 64; smaller tile avoids pipeline/LDS resource limits\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    # Robust reshape back to the original logical shape (contiguous 1D -> shape).\n    return o.reshape(a.shape)", "wall_us": 152.9, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    # Guard against empty tensors: nothing to launch, just reshape.\n    if numel == 0:\n        return o.reshape(a.shape)\n\n    # Memory-bound pointwise op: larger tile increases per-program memory-level\n    # parallelism and cuts grid/launch overhead, helping saturate HBM bandwidth.\n    BLOCK = 4096  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    # Robust reshape back to the original logical shape (handles any contiguity).\n    return o.reshape(a.shape)", "wall_us": 152.4, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\n\nPointwise FUSION out = f(a, b, c) computed in ONE pass (vs torch-eager multi-kernel).\nRegenerate via kore/tasks/generate_ops.py \u2014 do not hand-edit.\n\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    # Each program processes UNROLL contiguous tiles of size BLOCK.\n    # This raises memory-level parallelism (many in-flight wide loads per\n    # program) so HBM stays saturated with fewer, larger programs \u2014 the right\n    # regime for a bandwidth-bound pointwise fusion on MI325X.\n    pid = tl.program_id(0)\n    base = pid * (BLOCK * UNROLL)\n\n    for u in tl.static_range(UNROLL):\n        offs = base + u * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    o = torch.empty_like(a_flat)\n    numel = a_flat.numel()\n\n    # Guard against empty tensors: nothing to launch, just reshape.\n    if numel == 0:\n        return o.reshape(a.shape)\n\n    # Structural change: vectorized grid-stride via per-program UNROLL.\n    # BLOCK stays a multiple of 64 (wavefront-aligned); UNROLL fans out\n    # multiple independent wide loads to hide HBM latency.\n    BLOCK = 1024          # multiple of 64\n    UNROLL = 8            # tiles per program -> deep MLP of in-flight loads\n    grid = (triton.cdiv(numel, BLOCK * UNROLL),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL,\n        num_warps=4, num_stages=2,\n    )\n    # Robust reshape back to the original logical shape (handles any contiguity).\n    return o.reshape(a.shape)", "wall_us": 155.5, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the original logical shape, then use a flat view for the kernel.\n    o = torch.empty(a.shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    BLOCK = 1024  # multiple of 64; smaller tile avoids pipeline/LDS resource limits\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 151.29999999999998, "snr_db": 999.0, "rank": 0}], "preferences": [[0, 4], [1, 4], [2, 4], [3, 4], [5, 4]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "72b900182d6837da96cbfe73255f34808910844f00ff39884112d0c2e521709a", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the original logical shape, then use a flat view for the kernel.\n    o = torch.empty(a.shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    # Larger tile => more in-flight loads per wavefront => better MLP / HBM\n    # bandwidth saturation for this memory-bound pointwise kernel.\n    BLOCK = 4096  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 153.10000000000002, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    num_programs = tl.num_programs(0)\n    # Each program strides across the array, processing UNROLL tiles per step.\n    # This keeps many independent loads in flight (memory-level parallelism)\n    # while using far fewer, fatter waves to saturate HBM bandwidth.\n    step = BLOCK * num_programs\n    base = pid * BLOCK\n    for u in tl.static_range(UNROLL):\n        offs = base + u * step + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the original logical shape, then use a flat view for the kernel.\n    o = torch.empty(a.shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    BLOCK = 1024   # multiple of 64; per-tile vector width\n    UNROLL = 8     # tiles processed per program via grid-stride loop\n\n    total_tiles = triton.cdiv(numel, BLOCK)\n    # Launch enough programs to cover the array in UNROLL strided passes.\n    grid_programs = triton.cdiv(total_tiles, UNROLL)\n    grid = (grid_programs,)\n\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL, num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 154.9, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so all three operands agree in numel and layout.\n    a, b, c = torch.broadcast_tensors(a, b, c)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the broadcasted logical shape, then use a flat view.\n    o = torch.empty(a.shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    BLOCK = 1024  # multiple of 64; smaller tile avoids pipeline/LDS resource limits\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 150.9, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the original logical shape, then use a flat view for the kernel.\n    o = torch.empty(a.shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    # Memory-bound elementwise op: enlarge the tile so each wavefront issues more\n    # in-flight vectorized global loads, boosting HBM bandwidth utilization and\n    # cutting grid launch count. 8192 is a multiple of 64.\n    BLOCK = 8192\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o", "wall_us": 151.9, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    num_programs = tl.num_programs(0)\n\n    # Each program strides across the whole array in chunks of BLOCK,\n    # processing UNROLL independent chunks per iteration to expose\n    # more in-flight HBM loads (memory-level parallelism).\n    step = BLOCK * num_programs\n    base = pid * BLOCK\n\n    # Grid-stride loop: fewer, wider programs saturate memory bandwidth.\n    for i in range(0, tl.cdiv(numel, step * UNROLL) * UNROLL):\n        offs = base + (i * num_programs * BLOCK) + tl.arange(0, BLOCK)\n        # Re-derive offset so consecutive unrolled iters hit distinct chunks.\n        pass\n\n    # Simpler explicit unroll: launch a bounded loop over the whole domain.\n    offs0 = base + tl.arange(0, BLOCK)\n    for _ in range(0, tl.cdiv(numel, step)):\n        mask = offs0 < numel\n        a = tl.load(a_ptr + offs0, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs0, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs0, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs0, o, mask=mask)\n        offs0 += step\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a.contiguous().view(-1)\n    b_flat = b.contiguous().view(-1)\n    c_flat = c.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the original logical shape, then use a flat view for the kernel.\n    o = torch.empty(a.shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    BLOCK = 2048  # wider tile (multiple of 64) for better coalescing per program\n    UNROLL = 1\n\n    # Grid-stride launch: cap the number of programs so each does real work,\n    # keeping the CU array busy without oversubscribing the scheduler.\n    # Target enough programs to fill all CUs several times over.\n    max_programs = triton.cdiv(numel, BLOCK)\n    grid_programs = min(max_programs, 8192)\n    grid = (grid_programs,)\n\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL, num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 152.4, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the broadcasted logical shape, then use a flat view.\n    o = torch.empty(out_shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    BLOCK = 1024  # multiple of 64; smaller tile avoids pipeline/LDS resource limits\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 149.79999999999998, "snr_db": 999.0, "rank": 0}], "preferences": [[0, 1], [2, 1], [3, 1], [4, 1], [5, 1]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "74feafb77e039d263fe36ed33b41c28856c5e2a4da1fa91c704ce514732df31d", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the broadcasted logical shape, then use a flat view.\n    o = torch.empty(out_shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    # Memory-bound elementwise: use a larger tile and wider warps so each\n    # program issues more in-flight HBM transactions to hide memory latency.\n    BLOCK = 4096  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o", "wall_us": 150.2, "snr_db": 999.0, "rank": 0}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program handles UNROLL consecutive BLOCK-wide tiles for wide,\n    # coalesced HBM traffic and better memory-level parallelism per wavefront.\n    base = pid * BLOCK * UNROLL\n    for u in tl.static_range(UNROLL):\n        offs = base + u * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the broadcasted logical shape, then use a flat view.\n    o = torch.empty(out_shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    # Structural change: fatter programs. Each program processes UNROLL tiles of\n    # BLOCK elements, cutting launch overhead and increasing per-wavefront MLP so\n    # HBM stays saturated with fewer, wider in-flight requests.\n    BLOCK = 1024      # multiple of 64\n    UNROLL = 8        # tiles per program -> 8192 elems/program\n    grid = (triton.cdiv(numel, BLOCK * UNROLL),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL, num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 154.4, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    # Cast the fp32 accumulator back to the output tensor's dtype before storing.\n    tl.store(o_ptr + offs, o.to(o_ptr.dtype.element_ty), mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the broadcasted logical shape, then use a flat view.\n    o = torch.empty(out_shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    BLOCK = 1024  # multiple of 64; smaller tile avoids pipeline/LDS resource limits\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 152.79999999999998, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the broadcasted logical shape, then use a flat view.\n    o = torch.empty(out_shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    # Memory-bound pointwise: enlarge tile so each lane issues more independent\n    # loads, improving latency hiding and HBM bandwidth utilization.\n    BLOCK = 4096  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 152.0, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program handles UNROLL contiguous BLOCK-sized chunks, keeping many\n    # independent loads in flight per wavefront to saturate HBM bandwidth.\n    base = pid * BLOCK * UNROLL\n    for u in tl.static_range(UNROLL):\n        offs = base + u * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the broadcasted logical shape, then use a flat view.\n    o = torch.empty(out_shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    BLOCK = 1024   # multiple of 64\n    UNROLL = 8     # contiguous chunks per program -> more in-flight loads, fewer launches\n    grid = (triton.cdiv(numel, BLOCK * UNROLL),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL, num_warps=4, num_stages=2,\n    )\n    return o", "wall_us": 153.79999999999998, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    # Cast fp32 accumulator back to the destination element type before storing.\n    o = o.to(o_ptr.dtype.element_ty)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the broadcasted logical shape, then use a flat view.\n    o = torch.empty(out_shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    BLOCK = 1024  # multiple of 64; smaller tile avoids pipeline/LDS resource limits\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o", "wall_us": 152.79999999999998, "snr_db": 999.0, "rank": 3}], "preferences": [[0, 1], [0, 4], [2, 1], [2, 4], [3, 1], [3, 4], [5, 1], [5, 4]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "82f3e53100fd0f611c23436b00eebc6ab4a41d90cff63cee059721751fc3f76c", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the broadcasted logical shape, then use a flat view.\n    o = torch.empty(out_shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    # Memory-bound elementwise: enlarge the tile so each of the 512 lanes\n    # (8 warps x 64) processes multiple contiguous fp32 elements. This raises\n    # elements-per-thread, letting the backend emit wider (128-bit) vectorized\n    # global loads/stores that saturate MI325X HBM bandwidth more effectively.\n    BLOCK = 8192  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o", "wall_us": 152.79999999999998, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, GRID: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Grid-stride loop: each program sweeps multiple tiles so a small,\n    # occupancy-tuned grid keeps HBM saturated with many in-flight loads.\n    stride = GRID * BLOCK\n    base = pid * BLOCK\n    for start in tl.range(base, numel, stride):\n        offs = start + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the broadcasted logical shape, then use a flat view.\n    o = torch.empty(out_shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    # Memory-bound elementwise. Instead of one-tile-per-program, we launch a\n    # fixed-size grid sized to the machine (persistent-style) and let each\n    # program grid-stride over the data. This amortizes launch/prologue cost\n    # and keeps a steady stream of in-flight HBM transactions per CU.\n    BLOCK = 4096  # multiple of 64\n\n    # MI325X has 304 CUs; target a few waves of programs per CU so the\n    # scheduler always has work to hide memory latency, but cap the grid so\n    # each program does enough sequential work to amortize overhead.\n    n_tiles = triton.cdiv(numel, BLOCK)\n    GRID = min(n_tiles, 304 * 8)  # persistent-ish grid, bounded by work\n\n    grid = (GRID,)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel,\n        BLOCK=BLOCK, GRID=GRID, num_warps=8, num_stages=2,\n    )\n    return o", "wall_us": 154.70000000000002, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate a FLAT contiguous output buffer, launch on it, then reshape.\n    # This guarantees the store offsets are valid and contiguous regardless\n    # of the logical broadcasted shape's stride layout.\n    o_flat = torch.empty(numel, dtype=a_flat.dtype, device=a_flat.device)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o_flat.view(out_shape)\n\n    # Memory-bound elementwise: use a larger tile and wider warps so each\n    # program issues more in-flight HBM transactions to hide memory latency.\n    BLOCK = 4096  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o_flat.view(out_shape)", "wall_us": 151.2, "snr_db": 999.0, "rank": 0}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the broadcasted logical shape, then use a flat view.\n    o = torch.empty(out_shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    # Memory-bound elementwise: use a larger tile and wider warps so each\n    # program issues more in-flight HBM transactions to hide memory latency.\n    # Deepen the software pipeline (num_stages=4) so the many outstanding\n    # global loads overlap better and HBM latency is hidden more effectively.\n    BLOCK = 4096  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=8, num_stages=4\n    )\n    return o", "wall_us": 151.4, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    num_prog = tl.num_programs(0)\n    # Each program strides across the array, processing UNROLL tiles per step\n    # so a fixed occupancy-tuned grid keeps many HBM transactions in flight.\n    step = BLOCK * num_prog\n    base = pid * BLOCK\n    for u in tl.static_range(UNROLL):\n        offs = base + u * step + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the broadcasted logical shape, then use a flat view.\n    o = torch.empty(out_shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.view(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    # Memory-bound elementwise. Instead of launching one program per tile\n    # (which for large tensors spawns a huge grid dominated by launch/index\n    # overhead), launch a fixed grid sized to saturate the MI325X and let each\n    # program walk the array in a grid-stride loop. UNROLL tiles per program\n    # exposes memory-level parallelism (multiple in-flight HBM loads) while a\n    # bounded grid keeps scheduling overhead low.\n    BLOCK = 8192          # multiple of 64\n    UNROLL = 8            # tiles processed per program via grid-stride\n\n    total_tiles = triton.cdiv(numel, BLOCK)\n    # Cap the grid so each program does ~UNROLL tiles; MI325X has 304 CUs, so\n    # aim for a few thousand wavefront-groups to keep all CUs busy.\n    max_grid = triton.cdiv(total_tiles, UNROLL)\n    grid = (min(max_grid, 4096 * 8),)\n\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL, num_warps=8, num_stages=2,\n    )\n    return o", "wall_us": 156.4, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().reshape(-1)\n    b_flat = b_b.contiguous().reshape(-1)\n    c_flat = c_b.contiguous().reshape(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate output on the broadcasted logical shape, then use a flat view.\n    o = torch.empty(out_shape, dtype=a_flat.dtype, device=a_flat.device)\n    o_flat = o.reshape(-1)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o\n\n    # Memory-bound elementwise: use a larger tile and wider warps so each\n    # program issues more in-flight HBM transactions to hide memory latency.\n    BLOCK = 4096  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o", "wall_us": 152.60000000000002, "snr_db": 999.0, "rank": 1}], "preferences": [[0, 4], [1, 4], [2, 1], [2, 4], [3, 4], [5, 4]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "71fc1287cd5641c9b152a50fc55fae1c520186b1741161fab54c80e0b2563cc7", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate a FLAT contiguous output buffer, launch on it, then reshape.\n    # This guarantees the store offsets are valid and contiguous regardless\n    # of the logical broadcasted shape's stride layout.\n    o_flat = torch.empty(numel, dtype=a_flat.dtype, device=a_flat.device)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o_flat.view(out_shape)\n\n    # Memory-bound elementwise: enlarge the tile so each program issues more\n    # in-flight HBM transactions (higher memory-level parallelism) and fewer\n    # blocks are launched, cutting launch overhead. Larger BLOCK gives each\n    # of the 512 lanes (8 warps * 64) more independent loads to hide latency.\n    BLOCK = 16384  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o_flat.view(out_shape)", "wall_us": 155.6, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, TILES_PER_PROG: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    num_progs = tl.num_programs(0)\n    # Each program strides across the whole array, handling TILES_PER_PROG\n    # contiguous BLOCK-sized chunks per grid step. This raises the number of\n    # in-flight HBM loads per wavefront and amortizes launch overhead, letting\n    # a smaller grid saturate memory bandwidth on the MI325X.\n    stride = num_progs * BLOCK\n    base = pid * BLOCK\n    for t in tl.static_range(TILES_PER_PROG):\n        block_start = base + t * stride\n        offs = block_start + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    o_flat = torch.empty(numel, dtype=a_flat.dtype, device=a_flat.device)\n\n    if numel == 0:\n        return o_flat.view(out_shape)\n\n    # Memory-bound elementwise: each program now processes TILES_PER_PROG\n    # BLOCK-sized chunks in a grid-stride loop, increasing per-wavefront\n    # memory-level parallelism and reducing the total number of launched\n    # programs (less scheduling overhead).\n    BLOCK = 4096  # multiple of 64\n    TILES_PER_PROG = 4\n\n    total_tiles = triton.cdiv(numel, BLOCK)\n    # Launch enough programs to cover all tiles given TILES_PER_PROG stride.\n    num_progs = triton.cdiv(total_tiles, TILES_PER_PROG)\n    grid = (num_progs,)\n\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel,\n        BLOCK=BLOCK, TILES_PER_PROG=TILES_PER_PROG,\n        num_warps=8, num_stages=2,\n    )\n    return o_flat.view(out_shape)", "wall_us": 155.20000000000002, "snr_db": 999.0, "rank": 5}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate a FLAT contiguous output buffer, launch on it, then reshape.\n    o_flat = torch.empty(numel, dtype=a_flat.dtype, device=a_flat.device)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o_flat.view(out_shape)\n\n    # Memory-bound elementwise: use a moderate tile (multiple of 64) with\n    # num_warps=4 to keep VGPR pressure low and ensure a clean launch that\n    # maps tiles onto 64-lane wavefronts on gfx942.\n    BLOCK = 1024  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o_flat.view(out_shape)", "wall_us": 150.29999999999998, "snr_db": 999.0, "rank": 0}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate a FLAT contiguous output buffer, launch on it, then reshape.\n    # This guarantees the store offsets are valid and contiguous regardless\n    # of the logical broadcasted shape's stride layout.\n    o_flat = torch.empty(numel, dtype=a_flat.dtype, device=a_flat.device)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o_flat.view(out_shape)\n\n    # Memory-bound elementwise: enlarge the per-program tile so each of the\n    # 512 lanes (8 warps * 64) issues many independent in-flight HBM loads,\n    # raising memory-level parallelism to better hide deep HBM latency on\n    # MI325X. 8192 = 16 elements/lane, still a multiple of 64.\n    BLOCK = 8192  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=8, num_stages=2\n    )\n    return o_flat.view(out_shape)", "wall_us": 153.79999999999998, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, VEC: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program processes VEC contiguous sub-tiles of size BLOCK.\n    # This raises memory-level parallelism: many independent wide loads are\n    # in flight per program, better hiding HBM latency than one load/lane.\n    base = pid * (BLOCK * VEC)\n    lane = tl.arange(0, BLOCK)\n\n    for v in tl.static_range(VEC):\n        offs = base + v * BLOCK + lane\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    o_flat = torch.empty(numel, dtype=a_flat.dtype, device=a_flat.device)\n\n    if numel == 0:\n        return o_flat.view(out_shape)\n\n    # Memory-bound elementwise. Give each program VEC independent BLOCK-sized\n    # sub-tiles so multiple wide HBM transactions are in flight per program,\n    # amortizing launch/address overhead and improving memory-level parallelism.\n    BLOCK = 1024          # multiple of 64, maps to 16 wavefronts of work per sub-tile\n    VEC = 8               # sub-tiles per program -> 8192 elems/program\n    ELEMS_PER_PROG = BLOCK * VEC\n    grid = (triton.cdiv(numel, ELEMS_PER_PROG),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel,\n        BLOCK=BLOCK, VEC=VEC, num_warps=8, num_stages=2,\n    )\n    return o_flat.view(out_shape)", "wall_us": 155.9, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate a FLAT contiguous output buffer, launch on it, then reshape.\n    o_flat = torch.empty(numel, dtype=a_flat.dtype, device=a_flat.device)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o_flat.view(out_shape)\n\n    # Memory-bound elementwise op. Use a conservative tile + warp count that\n    # maps cleanly onto 64-lane wavefronts and avoids launch/occupancy issues.\n    BLOCK = 1024  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o_flat.view(out_shape)", "wall_us": 151.2, "snr_db": 999.0, "rank": 1}], "preferences": [[2, 0], [2, 1], [2, 3], [2, 4], [3, 0], [3, 1], [3, 4], [5, 0], [5, 1], [5, 3], [5, 4]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
{"task_id": "gen_add_add_relu_fp32", "parent_id": "f3ae646adc78eaf823408fb5dda0ed430e73a39f812bb8f80918e6336021289f", "candidates": [{"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate a FLAT contiguous output buffer, launch on it, then reshape.\n    o_flat = torch.empty(numel, dtype=a_flat.dtype, device=a_flat.device)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o_flat.view(out_shape)\n\n    # Memory-bound elementwise: use a larger tile (multiple of 64) so each\n    # program issues more in-flight global loads, improving memory-level\n    # parallelism and better hiding HBM latency on gfx942. num_warps=4 keeps\n    # VGPR pressure reasonable while each lane processes 64 elements.\n    BLOCK = 4096  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o_flat.view(out_shape)", "wall_us": 154.29999999999998, "snr_db": 999.0, "rank": 2}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program owns a contiguous super-tile of BLOCK*UNROLL elements.\n    base = pid * (BLOCK * UNROLL)\n    for u in tl.static_range(UNROLL):\n        offs = base + u * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate a FLAT contiguous output buffer, launch on it, then reshape.\n    o_flat = torch.empty(numel, dtype=a_flat.dtype, device=a_flat.device)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o_flat.view(out_shape)\n\n    # Memory-bound elementwise. Architectural change: each program now processes\n    # a super-tile of BLOCK*UNROLL elements via a statically-unrolled loop.\n    # This keeps many independent global loads in flight per wavefront (better\n    # HBM latency hiding / MLP), amortizes launch + index overhead across a\n    # larger work quantum, and reduces the total grid size (fewer WG dispatches).\n    BLOCK = 1024   # multiple of 64\n    UNROLL = 8     # elements per program = 8192\n    grid = (triton.cdiv(numel, BLOCK * UNROLL),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL, num_warps=4, num_stages=2,\n    )\n    return o_flat.view(out_shape)", "wall_us": 157.10000000000002, "snr_db": 999.0, "rank": 4}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().reshape(-1)\n    b_flat = b_b.contiguous().reshape(-1)\n    c_flat = c_b.contiguous().reshape(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate a FLAT contiguous output buffer, launch on it, then reshape.\n    o_flat = torch.empty(numel, dtype=a_flat.dtype, device=a_flat.device)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o_flat.reshape(out_shape)\n\n    # Memory-bound elementwise: use a moderate tile (multiple of 64) with\n    # num_warps=4 to keep VGPR pressure low and ensure a clean launch that\n    # maps tiles onto 64-lane wavefronts on gfx942.\n    BLOCK = 1024  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o_flat.reshape(out_shape)", "wall_us": 151.60000000000002, "snr_db": 999.0, "rank": 0}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate a FLAT contiguous output buffer, launch on it, then reshape.\n    o_flat = torch.empty(numel, dtype=a_flat.dtype, device=a_flat.device)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o_flat.view(out_shape)\n\n    # Memory-bound elementwise: use a larger tile (multiple of 64) to increase\n    # in-flight bytes per wavefront and better hide HBM latency on gfx942.\n    BLOCK = 4096  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o_flat.view(out_shape)", "wall_us": 152.79999999999998, "snr_db": 999.0, "rank": 1}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(\n    a_ptr, b_ptr, c_ptr, o_ptr, numel,\n    BLOCK: tl.constexpr, UNROLL: tl.constexpr,\n):\n    pid = tl.program_id(0)\n    # Each program handles UNROLL independent BLOCK-wide chunks.\n    # Laying the chunks out contiguously per-program maximizes coalescing\n    # while issuing multiple independent load streams to hide HBM latency.\n    base = pid * BLOCK * UNROLL\n    for u in tl.static_range(UNROLL):\n        offs = base + u * BLOCK + tl.arange(0, BLOCK)\n        mask = offs < numel\n        a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n        o = tl.maximum(a + b + c, 0.0)\n        tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate a FLAT contiguous output buffer, launch on it, then reshape.\n    o_flat = torch.empty(numel, dtype=a_flat.dtype, device=a_flat.device)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o_flat.view(out_shape)\n\n    # Memory-bound elementwise: each program processes UNROLL independent\n    # BLOCK-wide chunks. This raises memory-level parallelism (more loads in\n    # flight per wavefront) to better saturate HBM bandwidth, and shrinks the\n    # grid so launch/scheduling overhead drops. BLOCK stays a multiple of 64\n    # for clean mapping onto 64-lane wavefronts on gfx942.\n    BLOCK = 1024  # multiple of 64\n    UNROLL = 4    # independent chunks per program -> deeper memory pipeline\n    elems_per_program = BLOCK * UNROLL\n    grid = (triton.cdiv(numel, elems_per_program),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel,\n        BLOCK=BLOCK, UNROLL=UNROLL, num_warps=4, num_stages=2,\n    )\n    return o_flat.view(out_shape)", "wall_us": 154.6, "snr_db": 999.0, "rank": 3}, {"source": "\"\"\"GENERATED seed Triton kernel for the add_add_relu (fp32) fusion.\"\"\"\nfrom __future__ import annotations\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _add_add_relu_kernel(a_ptr, b_ptr, c_ptr, o_ptr, numel, BLOCK: tl.constexpr):\n    pid = tl.program_id(0)\n    offs = pid * BLOCK + tl.arange(0, BLOCK)\n    mask = offs < numel\n    a = tl.load(a_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    b = tl.load(b_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    c = tl.load(c_ptr + offs, mask=mask, other=0.0).to(tl.float32)\n    o = tl.maximum(a + b + c, 0.0)\n    tl.store(o_ptr + offs, o, mask=mask)\n\n\ndef add_add_relu(a: torch.Tensor, b: torch.Tensor, c: torch.Tensor) -> torch.Tensor:\n    # Broadcast to a common shape so element counts match before flattening.\n    out_shape = torch.broadcast_shapes(a.shape, b.shape, c.shape)\n\n    # Promote to a common dtype so mixed-precision inputs match the output.\n    out_dtype = torch.result_type(torch.result_type(a, b), c)\n\n    a_b = a.broadcast_to(out_shape)\n    b_b = b.broadcast_to(out_shape)\n    c_b = c.broadcast_to(out_shape)\n\n    # Flatten to 1D contiguous so pointwise indexing is unambiguous and aligned.\n    a_flat = a_b.contiguous().view(-1)\n    b_flat = b_b.contiguous().view(-1)\n    c_flat = c_b.contiguous().view(-1)\n\n    numel = a_flat.numel()\n\n    # Allocate a FLAT contiguous output buffer, launch on it, then reshape.\n    o_flat = torch.empty(numel, dtype=out_dtype, device=a_flat.device)\n\n    # Guard against empty tensors: nothing to launch.\n    if numel == 0:\n        return o_flat.view(out_shape)\n\n    # Memory-bound elementwise: use a moderate tile (multiple of 64) with\n    # num_warps=4 to keep VGPR pressure low and ensure a clean launch that\n    # maps tiles onto 64-lane wavefronts on gfx942.\n    BLOCK = 1024  # multiple of 64\n    grid = (triton.cdiv(numel, BLOCK),)\n    _add_add_relu_kernel[grid](\n        a_flat, b_flat, c_flat, o_flat, numel, BLOCK=BLOCK, num_warps=4, num_stages=2\n    )\n    return o_flat.view(out_shape)", "wall_us": null, "snr_db": -999.0, "rank": 5}], "preferences": [[0, 1], [0, 5], [1, 5], [2, 1], [2, 5], [3, 1], [3, 5], [4, 1], [4, 5]], "type": "ranked_group", "gpu": "gfx942", "operation": "add_add_relu", "arch": "gfx942", "shape": null}
